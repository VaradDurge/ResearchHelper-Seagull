/**
 * # LoadingSpinner Component
 * 
 * ## What it does:
 * Displays a loading spinner/indicator for async operations throughout the application.
 * Used during API calls, data fetching, and processing states.
 * 
 * ## How it works:
 * - Renders a spinning animation using CSS or a library like lucide-react
 * - Can be customized with size variants (sm, md, lg)
 * - Optional text label can be displayed below spinner
 * - Uses shadcn/ui Skeleton component or custom spinner
 * 
 * ## What to include:
 * - Spinner animation (CSS animation or icon-based)
 * - Size variants (small, medium, large)
 * - Optional loading text prop
 * - Accessibility: aria-label for screen readers
 * - Centered layout option
 * - Overlay variant (for full-page loading)
 */

