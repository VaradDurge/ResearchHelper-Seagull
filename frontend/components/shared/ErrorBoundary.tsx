/**
 * # ErrorBoundary Component
 * 
 * ## What it does:
 * React Error Boundary component that catches JavaScript errors in child components,
 * logs them, and displays a fallback UI instead of crashing the entire app.
 * 
 * ## How it works:
 * - Uses React Error Boundary API (class component or react-error-boundary library)
 * - Catches errors during rendering, lifecycle methods, and constructors
 * - Displays user-friendly error message
 * - Logs error details for debugging
 * - Provides reset/retry functionality
 * 
 * ## What to include:
 * - Error state management
 * - Fallback UI component
 * - Error logging (console or error reporting service)
 * - Reset error state method
 * - Optional error reporting to monitoring service
 * - User-friendly error message
 * - "Try again" button
 */

