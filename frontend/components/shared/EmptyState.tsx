/**
 * # EmptyState Component
 * 
 * ## What it does:
 * Displays empty state UI when there's no data to show (e.g., no papers uploaded,
 * no chat messages, no results). Provides context and actionable next steps.
 * 
 * ## How it works:
 * - Receives props: title, description, icon, action button
 * - Renders centered empty state with icon, text, and optional CTA
 * - Used across multiple pages (chat, PDF list, tool results)
 * 
 * ## What to include:
 * - Icon/image display
 * - Title text (e.g., "Upload a PDF to start chatting")
 * - Description text
 * - Optional action button (e.g., "Upload PDF", "Start Chat")
 * - Variants for different contexts (no papers, no results, no messages)
 * - Props interface for customization
 */

