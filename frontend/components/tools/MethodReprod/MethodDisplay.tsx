/**
 * # MethodDisplay Component
 * 
 * ## What it does:
 * Displays extracted method details from a paper. Shows structured sections:
 * Algorithm steps, Architecture, Training schedule, Hyperparameters, Experiments.
 * 
 * ## How it works:
 * - Receives extracted method data from API
 * - Renders each section with appropriate formatting
 * - Code blocks for algorithms
 * - Tables for hyperparameters
 * - Supports export functionality
 * 
 * ## What to include:
 * - Algorithm section (code blocks, step-by-step)
 * - Architecture section (text description, optional diagram)
 * - Training schedule section (timeline, phases)
 * - Hyperparameters section (table format)
 * - Experiments section (list of experiments with results)
 * - Export functionality (markdown, PDF)
 * - Copy section functionality
 * - Expandable/collapsible sections
 */

