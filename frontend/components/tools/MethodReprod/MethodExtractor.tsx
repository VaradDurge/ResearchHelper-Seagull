/**
 * # MethodExtractor Component
 * 
 * ## What it does:
 * Component for Method Reproduction tool. Paper selector (single paper) and
 * Extract button. Triggers method extraction from selected paper.
 * 
 * ## How it works:
 * - Single paper selector (dropdown or list)
 * - Extract button calls method extraction API
 * - Shows loading state during extraction
 * - Displays extracted method in MethodDisplay component
 * 
 * ## What to include:
 * - Paper selector (single select, not multi-select)
 * - Extract button
 * - Loading state during extraction
 * - Error handling
 * - Paper preview/metadata (optional)
 */

