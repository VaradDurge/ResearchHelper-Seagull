/**
 * # GraphViewer Component
 * 
 * ## What it does:
 * Renders interactive graph visualization showing method relationships across papers.
 * Shows nodes (techniques) and edges (relationships). Supports zoom, pan, and node selection.
 * 
 * ## How it works:
 * - Uses react-flow or vis.js for graph rendering
 * - Receives graph data from API (nodes, edges)
 * - Supports zoom, pan, node selection
 * - Click nodes to see details
 * - Handles layout algorithms (force-directed, hierarchical)
 * 
 * ## What to include:
 * - Graph canvas (react-flow or vis.js)
 * - Zoom controls (zoom in, zoom out, fit to view)
 * - Pan controls (drag to pan)
 * - Node click handlers (show details panel)
 * - Edge hover effects
 * - Legend for node/edge types
 * - Export as image (PNG, SVG)
 * - Node details panel (sidebar)
 * - Layout algorithm selector
 * - Search/filter nodes
 */

