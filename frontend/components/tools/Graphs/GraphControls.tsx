/**
 * # GraphControls Component
 * 
 * ## What it does:
 * Control panel for Graph tool. Paper selector (which papers to include),
 * Generate graph button, filter options, and layout options.
 * 
 * ## How it works:
 * - Paper selector (multi-select) for choosing papers
 * - Generate graph button calls graph generation API
 * - Filter options for node/edge types
 * - Layout options (force-directed, hierarchical, etc.)
 * 
 * ## What to include:
 * - Paper selector (multi-select checkboxes)
 * - "Generate Graph" button
 * - Filter options (node types: techniques, methods, etc.)
 * - Filter options (edge types: co-occurrence, influence, etc.)
 * - Layout algorithm selector (dropdown)
 * - Loading state during generation
 * - Reset filters button
 */

