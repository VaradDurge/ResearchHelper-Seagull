/**
 * # LLMSelector Component
 * 
 * ## What it does:
 * Multi-select checkboxes for LLM providers. Allows user to select which LLMs
 * to compare (OpenAI, Anthropic, Groq, etc.). Includes query input field.
 * 
 * ## How it works:
 * - Displays list of available LLM providers
 * - Multi-select checkboxes for each provider
 * - Query input field for the question to ask
 * - Submit button triggers cross-evaluation
 * 
 * ## What to include:
 * - List of LLM providers (OpenAI GPT-4, Anthropic Claude, Groq, etc.)
 * - Checkboxes for each provider
 * - Query textarea for the question
 * - Paper selector (which papers to use as context)
 * - Submit button ("Run Evaluation")
 * - Loading state
 * - Provider descriptions/help text
 */

