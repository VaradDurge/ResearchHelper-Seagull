/**
 * # ClaimInput Component
 * 
 * ## What it does:
 * Input component for the Claim Verification tool. Allows user to enter a claim
 * to verify against uploaded papers. Includes paper selector for choosing which
 * papers to check against.
 * 
 * ## How it works:
 * - Text input/textarea for claim entry
 * - Paper selector (multi-select) for choosing papers
 * - Submit button triggers verification API call
 * - Validates input before submission
 * 
 * ## What to include:
 * - Textarea for claim input
 * - Paper selector component (multi-select checkboxes or dropdown)
 * - Submit button ("Verify Claim")
 * - Input validation (non-empty claim, at least one paper selected)
 * - Loading state during verification
 * - Clear/reset functionality
 * - Example claims (optional helper text)
 */

