/**
 * # PDF Store (Zustand)
 * 
 * ## What it does:
 * Zustand store for managing PDF viewer state: current PDF, current page, zoom level,
 * and PDF viewer-related client state.
 * 
 * ## How it works:
 * - Uses Zustand for state management
 * - Stores current PDF ID and page
 * - Stores zoom level and viewer preferences
 * - Provides actions to update PDF viewer state
 * 
 * ## What to include:
 * - State: currentPaperId, currentPage, zoom, viewerPreferences
 * - Actions: setCurrentPDF, setPage, setZoom, setPreferences
 * - Persistence (optional, to remember zoom and preferences)
 * - TypeScript types for store state and actions
 */

