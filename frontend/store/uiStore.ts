/**
 * # UI Store (Zustand)
 * 
 * ## What it does:
 * Zustand store for managing UI state: sidebar open/closed, theme, modals, and
 * other UI-related client state.
 * 
 * ## How it works:
 * - Uses Zustand for state management
 * - Stores UI preferences and state
 * - Provides actions to update UI state
 * 
 * ## What to include:
 * - State: sidebarOpen, theme (light/dark), activeModal, notifications
 * - Actions: toggleSidebar, setTheme, openModal, closeModal, addNotification
 * - Persistence (optional, to remember theme and sidebar state)
 * - TypeScript types for store state and actions
 */

