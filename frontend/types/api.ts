/**
 * # API Types
 * 
 * ## What it does:
 * TypeScript types for API requests and responses. Defines the shape of data
 * exchanged between frontend and backend.
 * 
 * ## How it works:
 * - Exports TypeScript interfaces and types
 * - Matches backend Pydantic schemas
 * - Used by API functions and components
 * 
 * ## What to include:
 * - APIResponse<T> - Generic API response wrapper
 * - ErrorResponse - Error response structure
 * - PaginationResponse<T> - Paginated response structure
 * - Request types for each endpoint
 * - Response types for each endpoint
 * - Common types: ID, Timestamp, etc.
 */

