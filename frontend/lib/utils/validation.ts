/**
 * # Validation Utilities
 * 
 * ## What it does:
 * Utility functions for form validation, input validation, and data validation.
 * 
 * ## How it works:
 * - Exports validation functions
 * - Uses regex patterns and validation logic
 * - Returns validation results (boolean or error message)
 * 
 * ## What to include:
 * - validateDOI(doi: string): boolean - Validate DOI format
 * - validateEmail(email: string): boolean - Validate email format
 * - validateFileType(file: File, allowedTypes: string[]): boolean - Validate file type
 * - validateFileSize(file: File, maxSize: number): boolean - Validate file size
 * - validateRequired(value: any): boolean - Check if value is not empty
 * - Validation error messages
 */

