/**
 * # Format Utilities
 * 
 * ## What it does:
 * Utility functions for formatting dates, text, numbers, and other data for display.
 * 
 * ## How it works:
 * - Exports formatting functions
 * - Handles different formats and locales
 * - Provides consistent formatting across the app
 * 
 * ## What to include:
 * - formatDate(date: Date | string): string - Format dates (e.g., "Jan 15, 2024")
 * - formatRelativeTime(date: Date | string): string - Relative time (e.g., "2 hours ago")
 * - truncateText(text: string, maxLength: number): string - Truncate long text
 * - formatFileSize(bytes: number): string - Format file sizes (e.g., "2.5 MB")
 * - formatPageNumber(page: number): string - Format page numbers (e.g., "p. 12")
 * - formatAuthors(authors: string[]): string - Format author list
 */

